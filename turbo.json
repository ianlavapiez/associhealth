{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "env": ["DATABASE_ENCRYPTION_KEY", "DATABASE_HASH_SALT", "DATABASE_URL", "DATABASE_SSL"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
